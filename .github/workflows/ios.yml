name: iOS starter workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build and Test default scheme using any available iPhone simulator
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Resolve Package Dependencies
        run: |
          xcodebuild -resolvePackageDependencies
      
      - name: Set Default Scheme
        run: |
          scheme_list=$(xcodebuild -list -json 2>/dev/null)
          if [ -z "$scheme_list" ]; then
            echo "Warning: Could not get scheme list, using first available target"
            default=$(xcodebuild -list | grep "Schemes:" -A 100 | head -2 | tail -1 | xargs)
          else
            default=$(echo $scheme_list | ruby -e "require 'json'; data=JSON.parse(STDIN.gets); puts (data['project']['targets'][0] rescue data['workspace']['schemes'][0])")
          fi
          if [ -z "$default" ]; then
            echo "Error: Could not determine default scheme"
            exit 1
          fi
          echo $default | cat >default
          echo Using default scheme: $default
      
      - name: Build
        env:
          scheme: ${{ 'default' }}
          platform: ${{ 'iOS Simulator' }}
        run: |
          # xcrun xctrace returns via stderr, not the expected stdout (see https://developer.apple.com/forums/thread/663959)
          device=`xcrun xctrace list devices 2>&1 | grep "iPhone" | grep "Simulator" | head -1 | sed -E 's/[[:space:]]*\([^)]*\)[[:space:]]*$//' | awk '{$1=$1;print}'`
          if [ $scheme = default ]; then scheme=$(cat default); fi
          if [ "`ls -A | grep -i \.xcworkspace\$`" ]; then filetype_parameter="workspace" && file_to_build="`ls -A | grep -i \.xcworkspace\$`"; else filetype_parameter="project" && file_to_build="`ls -A | grep -i \.xcodeproj\$`"; fi
          file_to_build=`echo $file_to_build | awk '{$1=$1;print}'`
          xcodebuild build-for-testing -scheme "$scheme" -"$filetype_parameter" "$file_to_build" -destination "platform=$platform,name=iPhone 17 Pro"
      
      - name: Unit Test
        env:
          scheme: ${{ 'default' }}
          platform: ${{ 'iOS Simulator' }}
        run: |
          # xcrun xctrace returns via stderr, not the expected stdout (see https://developer.apple.com/forums/thread/663959)
          device=`xcrun xctrace list devices 2>&1 | grep "iPhone" | grep "Simulator" | head -1 | sed -E 's/[[:space:]]*\([^)]*\)[[:space:]]*$//' | awk '{$1=$1;print}'`
          if [ $scheme = default ]; then scheme=$(cat default); fi
          if [ "`ls -A | grep -i \.xcworkspace\$`" ]; then filetype_parameter="workspace" && file_to_build="`ls -A | grep -i \.xcworkspace\$`"; else filetype_parameter="project" && file_to_build="`ls -A | grep -i \.xcodeproj\$`"; fi
          file_to_build=`echo $file_to_build | awk '{$1=$1;print}'`
          xcodebuild test-without-building -scheme "{{ $scheme }}Tests" -"$filetype_parameter" "$file_to_build" -destination "platform=$platform,name=iPhone 17 Pro"
